import { render, screen } from "@testing-library/react";
import React from "react";
import PricingBox from "../Components/PricingBox";

test("on initial render, the component should have data", () => {
  // render(<PricingBox />);
});
